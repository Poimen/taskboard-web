// import { User } from 'store/models/user';
// import { AllActions } from 'store/reducers';

const setApiToken = (token: string) => ({ type: 'SET_API_TOKEN', token });

export default {
  setApiToken
};
