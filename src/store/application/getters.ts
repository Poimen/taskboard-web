import { GetterTree } from 'vuex';
import { ApplicationState } from './models/ApplicationState';
import { IRootState } from './models/IRootState';

export const getters: GetterTree<ApplicationState, IRootState> = {
};
